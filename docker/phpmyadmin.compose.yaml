services:
  phpmyadmin:
      image: phpmyadmin/phpmyadmin
      restart: unless-stopped
      ports:
      - "2001:80"
      environment:
        PMA_HOST : db
        PMA_PORTS : 3306
      networks:
        - Jdr-net